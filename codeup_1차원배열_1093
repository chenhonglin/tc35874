#include <stdio.h>
int main()
{
	int i, j, n,k;
	int a[24] = {};
	scanf_s("%d", &n);
	for (i = 1; i <= n; i++) {
		scanf_s("%d ", &k);
		a[k] = a[k] + 1;
	}
	for (j = 1; j < 24; j++) {
		printf("%d ", a[j]);
	}

	return 0;
}
