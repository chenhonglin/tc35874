#include <stdio.h>
#pragma warning(disable : 4996)


int main()
{
	int num[10] = {};
	int i, j, k;
	int *pnum;
	pnum = num;
	for (i = 0; i < 10; i++) {
		scanf("%d", &num[i]);
	}
	scanf("%d", &k);
	for (i = 0; i < 10; i++) {
		if (i == (k - 1))
			printf("%d", num[i]);
}
	
	return 0;
}
